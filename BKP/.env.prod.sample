# Django Configuration
DJANGO_SECRET_KEY=CHANGE_ME
DJANGO_DEBUG=False
ALLOWED_HOSTS=*
DJANGO_SETTINGS_MODULE=memoria.settings.prod

# Database Configuration (PostgreSQL)
POSTGRES_DB=acad_micros
POSTGRES_USER=acad_micros
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_HOST=acad_micros_postgres
POSTGRES_PORT=5432

# Alternative: Use DATABASE_URL instead of individual POSTGRES_* variables
# DATABASE_URL=postgresql://acad_micros:CHANGE_ME@acad_micros_postgres:5432/acad_micros

# CSRF Trusted Origins (adjust for your domain)
# CSRF_TRUSTED_ORIGINS=https://apps.dcc.uchile.cl,https://yourdomain.com

# Optional: Logging Level
# DJANGO_LOG_LEVEL=INFO

# Optional: Script Name for sub-path deployment
# SCRIPT_NAME=/docencia/acad_micros/
